{
  "name": "connect-reference-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "author": "masterzee001",
  "license": "SEE LICENSE IN ../../LICENSE.md",
  "description": "Connect Reference web app: rooms, lobby, and conference screens built entirely on the public @videofy/connect SDK against the Connect Reference server.",
  "scripts": {
    "dev": "vite",
    "build": "npm --prefix ../.. run build -w @videofy/connect && node scripts/check-vocab.mjs && tsc --noEmit && vite build",
    "preview": "vite preview",
    "check:vocab": "node scripts/check-vocab.mjs",
    "typecheck": "npm --prefix ../.. run build -w @videofy/connect && tsc --noEmit",
    "test": "npm --prefix ../.. run build -w @videofy/connect && node scripts/check-vocab.mjs && vitest run"
  },
  "dependencies": {
    "@videofy/connect": "*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.3.4",
    "vitest": "^2.0.5"
  }
}
