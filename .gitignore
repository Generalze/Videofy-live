# Logs
logs
.videofy-dev-logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# Snowpack dependency directory (https://snowpack.dev/)
web_modules/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variable files
.env
.env.*
!.env.example

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next
out

# Nuxt.js build / generate output
.nuxt
dist
.output

# Gatsby files
.cache/
# Comment in the public line in if your project uses Gatsby and not Next.js
# https://nextjs.org/blog/next-9-1#public-directory-support
# public

# vuepress build output
.vuepress/dist

# vuepress v2.x temp directory
.temp

# Sveltekit cache directory
.svelte-kit/

# vitepress build output
**/.vitepress/dist

# vitepress cache directory
**/.vitepress/cache

# Docusaurus cache and generated files
.docusaurus

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# Firebase cache directory
.firebase/

# TernJS port file
.tern-port

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# pnpm
.pnpm-store

# yarn v3
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Vite files
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.vite/

# Python virtual environments
venv/
.venv/
.venv-ai/
# Isolated OpenVoice V2 environment (Python 3.9 + CUDA torch). Multi-gigabyte,
# machine-specific, and deliberately never shared with .venv-ai.
.venv-openvoice/
# Reproduction environment built to prove the runtime installs from pinned
# sources without PYTHONPATH. Rebuildable, so never committed.
.venv-openvoice-clean/
# Large wheels staged for resumable download on a slow link.
.wheels/
# Stage-gate evidence for the OpenVoice spike: machine-specific pip/CUDA state.
.openvoice-evidence/
# Third-party OpenVoice / MeloTTS checkouts for the spike.
.openvoice-src/
# Derived personal-voice representations. Small, but they identify a person.
voice-assets/
env/
.env/
__pycache__/
*.pyc
*.pyo
*.pyd
*.pyw
.Python

# Python cache
*.egg-info/
.eggs/
dist/
build/
*.egg
pip-log.txt

# Temporary audio / generated speech
tmp/audio/
generated_speech/
recordings/
uploads/
# Personal voice enrollment material: biometric data about a specific person.
# It must never reach Git, and the directory is named here so an accidental
# `git add -A` cannot carry it in.
voice-enrollment/
*.enrollment.wav
*.wav
*.mp3
*.ogg
*.webm.tmp

# Local test videos
*.mp4
*.mkv
*.avi
sample_video.*

# Downloaded AI models
models/
model_cache/
*.gguf
*.bin
*.safetensors

# Logs
*.log

# Build output
dist/
build/

# Environment
.env
.env.local
.env.*.local

# Python test cache
.pytest_cache/
*.tsbuildinfo

# C-AI1 bake-off evidence: raw provider comparisons, kept local
.videofy-bakeoff/
# Visual-review captures: binary, regenerated on demand, and not evidence Git
# should carry.
.videofy-screenshots/

# Locally built engine wheels. Identified by hash in engine-sources.json and
# rebuildable from pinned commits, so never committed.
.engine-dist/

# Throwaway cold-start probe.
.cold-matrix.mjs

# Connect project registry: partner authorization data, never committed.
connect-projects.json
connect-projects.*.json

# npm pack artifacts
packages/**/*.tgz

# Connect Reference room registry: product data, never committed.
connect-reference-rooms.json

# Staging SPA build output (deploy/staging/build-apps.sh)
dist-staging/

# Service-account keys and other provider credentials.
#
# A Firebase admin SDK JSON carries a PRIVATE KEY. One `git add -A` puts it in
# history permanently, and history cannot be un-published once pushed -- the
# only remedy at that point is rotating the key at the provider. Ignored here as
# defence in depth; the file itself belongs OUTSIDE the repository.
firebase/
*-firebase-adminsdk-*.json
*service-account*.json
*.p8
